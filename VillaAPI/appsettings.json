{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Connection string for local SQL Server instance using Windows Authentication, 
  // connecting to 'MagicVillaAPI' database, trusting the server's SSL certificate, 
  // and enabling multiple active result sets.
  "ConnectionStrings": {
    "DefaultSQLConnections": "Server=.; Database=MagicVillaAPI; Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=true"
  }
}
